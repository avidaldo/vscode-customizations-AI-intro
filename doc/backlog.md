# Example Backlog Snapshot

> Example output generated by the `todo-a-plan` skill on 2026-06-08.
> This file is preserved as a sample artifact, not as the canonical current status of the repository.

## Summary

| Total | High Priority | Medium Priority | Low Priority |
| ----- | ------------- | --------------- | ------------ |
| 6     | 1             | 3               | 2            |

## Backlog

| # | Priority | Effort | Type | File | Line | Description | Depends On |
| - | -------- | ------ | ---- | ---- | ---- | ----------- | ---------- |
| 1 | High | Medium | Documentation | `doc/justificacion.md` | 5 | Expand each primitive entry with a concrete "How to use" subsection: what command to type, what to expect, how to verify the primitive is active in this repo. The current file explains *why* each primitive was chosen but not *how* to exercise it. | — |
| 2 | Medium | Medium | Refactor | `doc/justificacion.md` | 22 | Replace all audience-specific references to "students" with agnostic phrasing ("the developer", "the user of this template"). The repo should be readable by anyone exploring Copilot customizations, not only course participants. | — |
| 3 | Medium | Small | Documentation | `src/train_model.py` | 6 | Resolve the inline TODO by adding a brief module-level note (or a pointer to `doc/capas.md`) that explains which customization layers directly target this file: `python.instructions.md` (PEP 8 + NumPy docstrings), the `csv-eda-basica` skill (which uses the data this script trains on), and the `notebook-guardian` hook (which protects the notebook that visualises its outputs). | — |
| 4 | Medium | Small | Documentation | `notebooks/01_eda_example.ipynb` | 21 | Add a Markdown cell that makes the customization connections explicit: this notebook is governed by `notebooks.instructions.md`, protected by the `notebook-guardian` hook, and is the primary target of the `revision-notebook` skill. Resolves the inline TODO comment. | #3 (keep language consistent with the resolution of the `train_model.py` TODO) |
| 5 | Low | Small | Refactor | `doc/justificacion.md` | 8 | Remove the "Pedagogical insight:" label from all entries once #1 is done. If the "How to use" subsection and the "Why not X?" section are detailed enough, the separate heading is redundant and adds noise. | #1 |
| 6 | Low | Small | Documentation | `enunciado.md` | 128 | Add an optional "Going further" section after the required tasks, suggesting additional skills (`debug-python-basico`, `spec-a-tareas`, `dataset-card`) as stretch exercises for learners who want to explore beyond the mandatory deliverables. | — |

## Recommended Next Action

**Start with item #1** — expanding `doc/justificacion.md` with a concrete "How to use" subsection for each primitive.

This is the highest-value task because:

1. **It unblocks everything else.** Items #2 and #5 are cleanup tasks that only make sense after the content is complete.
2. **It is the core gap in the reference solution.** The file currently answers *why* each primitive exists but not *how* to invoke and verify it. A learner following this repo as a tutorial would hit this gap immediately.
3. **It directly serves the project's pedagogical goal.** The repo is a reference implementation; a reference that does not show concrete usage is incomplete by definition.

Once #1 is done, items #3 and #4 can be addressed together in one pass (same conceptual message, two files), then #2 and #5 as a final cleanup pass.
